import React from "react";
import {Box, Typography} from "@mui/material";


const NotFoundPage =()=>{

    return(<Box sx={{width:'100%',display:'flex',justifyContent:'center',alignItems:'center',height:'50vh'}}>
        <Typography sx={{color:'white',fontSize:30,fontWeight:700}}>Pagia no encontrada</Typography>
    </Box>)
}
export default NotFoundPage;
